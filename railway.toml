[build]
builder = "NIXPACKS"

[deploy]
startCommand = "streamlit run AisocRawAndStupid.py --server.port $PORT --server.headless true --server.runOnSave false"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"